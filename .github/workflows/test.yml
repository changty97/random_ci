name: Testing Mask
on:
  push:

jobs:
  test:
    runs-on: [ubuntu-latest]
    steps:
      - name: Mask GCP token
        run: |
          exec &> /dev/null
          token="123"
          echo "::add-mask::${{ env.token }}"
          echo "Token is: ${{ env.token }}"
